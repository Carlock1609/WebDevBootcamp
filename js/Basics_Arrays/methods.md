Push = append - adds element to end
pop = takes the last element
unshift = adds element to begining
shift = pops off first element

indexOf = .index["david"] would find the index
correct syntax would be friends.indexOf["Dave"]
if it is not in list, it comes back -1 which would be false

slice = fruits.slice(1,3) gets everything between writing fruits.slice() copies whole array
